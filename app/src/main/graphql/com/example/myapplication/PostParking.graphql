mutation postParking($parking: ParkingInputLoc!){
    par_createNewParkingLoc(parking: $parking){
        id
        name
        pricePerMinute
        totalSpaces
        usedSpaces
        openHours{
            opening
            closing
        }
        idLocation
        address
    }
}